name: Build and Deploy

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v2

      - name: Set up Node.js for React App
        uses: actions/setup-node@v3
        with:
          node-version: 16

      - name: Install dependencies for React App
        run: npm --prefix client/ install

      - name: Build React App
<<<<<<< HEAD
        run: npm --prefix client/ run build
=======
        run: |
          cd client/
          npm run build
          cd ..

      - name: Display React App Build Output
        run: |
          echo "Contents of client/dist/:"
          ls -R /
>>>>>>> a1c3940 (Update deploy.yml)

      - name: Set up Node.js for Server-Side App
        uses: actions/setup-node@v3
        with:
          node-version: 16

      - name: Install dependencies for Server-Side App
        run: npm --prefix server/ install

  deploy:
    needs: build
    runs-on: ubuntu-latest

    steps:
      - name: Deploy React App to cPanel
        uses: SamKirkland/FTP-Deploy-Action@v4.3.4 
        with:
          server: ftp.ethiobooking.com
          username: abebe@ethiobooking.com
          password: ${{ secrets.FTP_FRONTEND_PASSWORD }}
          server-dir: /abebe.ethiobooking.com/
<<<<<<< HEAD
          local-dir: client/dist/
=======
          
>>>>>>> 3ebb9ba (Update deploy.yml)
          exclude: ".git,.github,node_modules"

      - name: Deploy Node.js Server-Side App to cPanel
        uses: SamKirkland/FTP-Deploy-Action@v4.3.4
        with:
          server:  ftp.ethiobooking.com
          username: abebeserver@ethiobooking.com
          password: ${{ secrets.FTP_BACKEND_PASSWORD }}
          server-dir: /abebeapi.ethiobooking.com/
<<<<<<< HEAD
          local-dir: server/
=======
          
>>>>>>> 3ebb9ba (Update deploy.yml)
          exclude: ".git,.github,node_modules"
